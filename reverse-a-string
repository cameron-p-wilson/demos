//Reverses a string in O(n) time and O(1) space

#include <iostream>
#include <string>

int main()
{
	std::string s;
	std::cin >> s;
  
  	int j = s.size() - 1;
  	for (int i = 0; i < j; i++)
  	{
		char temp = s[i];
		s[i] = s[j];
		s[j] = temp;
  	}

	return 0;
}
